<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ziyan.recipe.mapper.UserRecipeLogMapper">

    <select id="findByUserIdAndMade" resultType="com.ziyan.recipe.entity.UserRecipeLog">
        SELECT *
        FROM user_recipe_logs
        WHERE user_id = #{userId}
        <if test="made != null">
            AND made = #{made}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="findMadeRecipeIdsByUserId" resultType="long">
        SELECT recipe_id
        FROM user_recipe_logs
        WHERE user_id = #{userId} AND made = TRUE
    </select>

</mapper>
